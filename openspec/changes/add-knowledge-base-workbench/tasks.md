## 1. 骨架与配置

- [ ] 1.1 建立 `src/kbwb/` 模块结构（`config/`、`providers/`、`acquire/`、`process/`、`profile/`、`kernel/`、`qa/`、`server/`、`eval/`）与 `tests/` 镜像结构，验证 `uv run python -c "import kbwb"` 成功
- [ ] 1.2 在 `pyproject.toml` 加入运行时依赖（`scrapling`、`fastapi`、`uvicorn`、`chromadb`、`bm25s`、中文分词库、`pyyaml`、`typer`）与可选依赖组 `local-models`（`sentence-transformers`）、`ocr`，验证 `uv sync --extra dev` 成功且各包可导入。结构化文档解析库待 6.0 对比后加入
- [ ] 1.3 用 pydantic-settings 实现环境配置模型（provider 选型与凭据、数据根目录、监听地址、切分与检索默认值、各类阈值），验证缺失必填项时抛出指明变量名的错误的单元测试通过
- [ ] 1.4 实现知识库存储布局与 `kb_id` 解析（`<data_root>/<kb_id>/{raw,md,index}`），验证目录创建、路径穿越拒绝的单元测试通过
- [ ] 1.5 定义站点配置 schema 与 YAML 加载校验，验证非法配置在加载阶段报出字段路径的单元测试通过
- [ ] 1.6 扩充 `.env.example` 覆盖全部新增变量，验证按其填写后 1.3 的配置加载通过

## 2. 模型 provider

- [ ] 2.1 先写 `ChatProvider` / `EmbeddingProvider` 契约测试（含未知 provider 名称在启动时报错并列出可用名称），验证测试因未实现而失败
- [ ] 2.2 定义两个接口与按名注册表，验证 2.1 契约测试转为通过
- [ ] 2.3 实现基于 OpenAI SDK 的 chat/embedding provider，以 `base_url` + 模型名 + 凭据变量区分 OpenAI 与 DeepSeek，验证两种配置的集成测试（录制响应）通过
- [ ] 2.4 实现本地 Sentence-Transformers embedding provider 并纳入可选依赖组，验证无外网条件下嵌入成功的测试通过
- [ ] 2.5 实现退避重试与错误分类，验证限流重试成功、重试耗尽返回可区分错误、不返回空向量冒充成功的单元测试通过
- [ ] 2.6 实现凭据校验与日志脱敏，验证认证失败信息不含凭据任何字符的单元测试通过

## 3. 站点分析

- [ ] 3.1 实现受 robots.txt 与速率限制约束的页面采样器，验证被 robots 禁止时不发起请求并报告规则的测试通过
- [ ] 3.2 实现候选选择器推断（含文本密度辅助判断正文），验证对固定 HTML 样本产出按命中率降序候选列表的测试通过
- [ ] 3.3 实现无可用候选时标记"需人工指定"，验证对应单元测试通过
- [ ] 3.4 实现站点配置草稿生成（低置信度字段带注释），验证草稿能通过 1.5 的配置校验的测试通过
- [ ] 3.5 实现已有配置不被静默覆盖的写入逻辑，验证重复运行时写入新路径并告知的测试通过

## 4. 通用爬虫

- [ ] 4.1 实现 robots.txt 解析缓存、可识别 User-Agent、每域名并发与间隔控制，验证并发数与最小间隔不被突破的测试通过
- [ ] 4.2 实现 URL 准入与排除规则、分页跟进，验证越界链接不被抓取的测试通过
- [ ] 4.3 实现基于 scrapling 的抓取与字段抽取（不启用其反检测能力），验证对固定样本抽取出预期字段的测试通过
- [ ] 4.4 实现 429/503 退避重试与放弃记录，验证达到重试上限后放弃且不持续施压的测试通过
- [ ] 4.5 实现页面快照、元数据与内容哈希落盘，验证 `raw/` 结构与哈希稳定的测试通过
- [ ] 4.6 实现附件下载、体积上限跳过与失败保留原始 URL，验证三种情形各自的测试通过
- [ ] 4.7 实现基于内容哈希的增量爬取与结果摘要统计，验证未变更页面不产生新产物、变更页面被标记待重处理的测试通过

## 5. 本地导入

- [ ] 5.1 实现文件夹递归扫描与包含/排除规则，验证子目录纳入、排除生效、路径不可读时报错的测试通过
- [ ] 5.2 实现基于内容而非扩展名的类型识别，验证扩展名与内容不符时按真实类型处理的测试通过
- [ ] 5.3 实现不受支持类型的标记与跳过报告，验证跳过项计入报告而非静默丢弃的测试通过
- [ ] 5.4 实现与爬取产物一致的统一文档记录，验证两种来源产出的记录字段结构一致的测试通过
- [ ] 5.5 实现增量导入与源文件删除识别，验证未变更文件跳过、已删除文件被列出并询问的测试通过

## 6. 文档处理

- [ ] 6.0 以不少于 10 份真实公文样本（含带表格的 PDF、DOCX 与扫描件）对比 Docling 与 MinerU，就表格行列保真、标题层级还原、中文版式与耗时四项打分，产出选型结论并写入变更记录，据此在 `pyproject.toml` 加入所选依赖
- [ ] 6.1 实现 HTML 到 Markdown 的转换与噪声剔除，验证结果保留标题层级、不含导航页脚的测试通过
- [ ] 6.2 实现正文为空或过短时标记提取失败，验证该文档不进入索引的测试通过
- [ ] 6.3 用 6.0 选定的解析库实现 PDF/DOCX/XLSX 到 Markdown 的解析，验证三种格式各自解析成功且表格行列对应关系被保留的测试通过
- [ ] 6.3a 实现复杂表格（含合并单元格）的结构保留与标记，验证其不被展平为连续文本的测试通过
- [ ] 6.3b 实现 `md/` 中间产物落盘与从原件重建，验证删除 `md/` 后可重新生成、且原件与对外附件地址不受影响的测试通过
- [ ] 6.3c 实现标题层级不可得时标记为平铺结构，验证 OCR 产物被标记为平铺且不伪造标题的测试通过
- [ ] 6.4 实现扫描件判别与可选 OCR 接入，验证无文本层 PDF 被判定为扫描件、启用 OCR 后可提取文本的测试通过
- [ ] 6.5 实现不支持类型与损坏文件的降级处理，验证元数据与来源标识被保留的测试通过
- [ ] 6.6 实现元数据抽取，验证缺失字段标记为未知而非以获取时间等猜测值填充的测试通过
- [ ] 6.7 基于 Markdown 中间表示实现按标题层级与段落边界的语义切分（含超长段落的句子边界拆分与重叠），验证小节不被跨界混合的测试通过
- [ ] 6.7a 实现表格作为原子单元不被切断、超长表格按行拆分时重复表头，验证两种情形各自的测试通过
- [ ] 6.7b 实现平铺结构文档退回按段落与长度切分，验证对应测试通过
- [ ] 6.8 实现片段继承来源文档全部元数据（含附件列表与来源标识），验证同一文档所有片段均带完整信息的测试通过

## 7. 语料画像与推荐器

- [ ] 7.1 实现语料画像统计（类型分布、扫描件占比、表格密集文档数、正文长度分布、重复模板数），验证各统计项对构造语料给出正确数值的测试通过
- [ ] 7.2 实现语料为空时返回明确状态而非全零画像，验证对应测试通过
- [ ] 7.3 实现编号/文号密度检测（正则族），验证对含文号样本检出、对普通文本不误报的测试通过
- [ ] 7.4 实现规则式推荐引擎，输出建议值 + 引用具体画像数值的理由，验证四条核心规则（OCR、BM25、chunk 大小、去重）各自触发的测试通过
- [ ] 7.5 实现推荐边界约束：标注为启发式、不跑评测、每项可覆盖并记录覆盖标记，验证覆盖后采用用户值且标记生效的测试通过
- [ ] 7.6 实现无法从语料推断的维度标记为"需使用者指定"，验证系统不给出推断值的测试通过
- [ ] 7.7 验证画像与推荐对未变更语料可重复产出一致结果

## 8. 知识内核

- [ ] 8.1 先写 KnowledgeKernel 契约测试（`ingest`/`retrieve`/`delete`、未知内核名启动报错、特有参数经配置透传），验证测试因未实现而失败
- [ ] 8.2 定义契约与注册表，验证 8.1 契约测试转为通过
- [ ] 8.3 实现向量通道（Chroma persistent client）与元数据持久化，验证检索命中结果携带完整元数据的测试通过
- [ ] 8.4 实现中文分词接入与文号原子化（复用 7.3 的正则族保护编号边界，同时保留切分形式作兜底），验证完整文号整体入索引、不被切碎、部分编号仍有召回的测试通过
- [ ] 8.4a 实现 BM25 关键词通道，验证以完整文号查询能召回含该编号片段、且其分值高于仅含数字片段文档的测试通过
- [ ] 8.5 实现两路融合与检索方式切换（纯向量/纯关键词/混合），验证三种模式各自行为的测试通过
- [ ] 8.6 实现可选 rerank 与"加载失败即中止不静默降级"，验证精排生效与失败中止两个测试通过
- [ ] 8.7 实现 K 值截断与相似度阈值过滤，验证降序排列、低于阈值不返回的测试通过
- [ ] 8.8 实现索引版本目录与生效指针的原子切换，验证重建期间检索走旧版本、失败或中断后指针不变、成功后切换的测试通过
- [ ] 8.9 实现按知识库隔离的索引存储，验证跨库检索不串数据的测试通过
- [ ] 8.10 在契约与配置中预留图谱内核位（不实现），验证选择图谱时返回"未在当前版本启用"的测试通过

## 9. 问答与导出服务

- [ ] 9.1 实现问答入参校验（空问题、超长、无生效索引），验证三种情形均在调用模型前返回明确错误的测试通过
- [ ] 9.2 实现检索为空时不调用生成模型直接拒答，验证响应明确且未发起模型调用的测试通过
- [ ] 9.3 实现答案生成与来源引用组装（网页来源给 URL、本地来源给路径），验证两类来源各自的测试通过
- [ ] 9.4 实现引用只含实际用作依据的片段，验证未被使用的检索结果不进入引用的测试通过
- [ ] 9.5 实现附件列表聚合与去重，验证多片段关联同一附件只出现一次、无附件返回空数组的测试通过
- [ ] 9.6 实现单库只读导出服务（绑定单一 kb_id、不暴露写接口、默认回环、速率限制），验证四项各自的测试通过
- [ ] 9.7 实现对外监听时的无鉴权提示，验证配置非回环地址时启动输出提示的测试通过

## 10. 工作台后端

- [ ] 10.1 实现本地服务启动、默认绑定回环、端口冲突时报错提示，验证三项各自的测试通过
- [ ] 10.2 实现多知识库管理接口（创建、列表、切换、删除前确认），验证删除只影响目标库的测试通过
- [ ] 10.3 实现后台长任务框架与状态持久化，验证任务在请求结束后继续运行、状态可查询的测试通过
- [ ] 10.4 实现任务中断，验证中断建库后生效索引版本不变的测试通过
- [ ] 10.5 实现进度查询接口，验证模拟"关闭并重开页面"后仍能取到当前进度的测试通过
- [ ] 10.6 串接爬取、导入、画像、建库、问答为工作台 API，验证各端点的集成测试通过

## 11. 工作台前端

- [ ] 11.1 搭建前端工程与构建产物由后端静态托管，验证访问本地 URL 能打开界面
- [ ] 11.2 实现界面一：站点配置、触发爬取、本地文件夹导入、实时进度与结果摘要展示，验证爬取与导入两条路径的端到端手工核对通过
- [ ] 11.3 在界面一展示语料画像（五类统计），验证展示数值与后端画像一致
- [ ] 11.4 实现界面二：展示推荐配置及其理由、逐项可覆盖、图谱选项置灰并说明，验证覆盖后建库采用用户值
- [ ] 11.5 在界面二展示建库进度与结果（片段数、耗时、版本标识），验证中断按钮生效且索引不受损
- [ ] 11.6 实现界面三：单次试答，展示答案、命中片段与分值、来源与附件，验证未命中时展示最接近候选及分值
- [ ] 11.7 在界面三实现批量评测运行与结果展示（总体与逐用例分值、失败用例可展开），验证展示与后端评测输出一致
- [ ] 11.7a 在界面三实现关注问题集的维护与体检结果展示，验证未命中问题可展开查看最接近候选及分值
- [ ] 11.7b 在界面三实现种子评测集生成入口与合成用例的编辑/删除，验证合成来源标记可见且编辑后标记消失
- [ ] 11.8 实现多知识库切换与删除确认交互，验证切换后展示内容属于当前库

## 12. 评测

- [ ] 12.1 定义评测集文件格式与加载校验，验证非法用例在评测前中止并指出用例序号与字段名的测试通过
- [ ] 12.2 接入 DeepEval 的忠实度、答案相关性、上下文召回三项指标（judge 走 DeepSeek），验证输出总体与逐用例分值的测试通过
- [ ] 12.3 实现附件命中判定，验证命中与缺失两种情形报告内容的测试通过
- [ ] 12.4 实现阈值回归判定与退出码，验证达标返回 0、未达标返回非 0 并指出未达标指标的测试通过
- [ ] 12.5 实现评测集本地持久化与从试答记录添加用例，验证重启后可加载、标注后用例被追加的测试通过
- [ ] 12.6 实现检索可达性体检（使用者提供问题集，对已建索引逐题报告命中、分值与来源），验证命中、低分命中与完全未命中三种情形被区分、且未提供问题时不自行生成的测试通过
- [ ] 12.7 实现从语料合成种子评测用例，验证用例携带来源片段、被标记为合成来源、可编辑后转为人工用例的测试通过
- [ ] 12.8 实现合成用例的声明约束，验证报告中输出"不代表真实查询分布"提示、全合成评测集额外提示仅可用于相对比较的测试通过

## 13. 集成验收与文档

- [ ] 13.1 端到端跑通「爬取站点 → 画像 → 采纳建议建库 → 试答」全流程，验证能得到带来源引用与附件直链的答案
- [ ] 13.2 端到端跑通「导入本地文件夹 → 画像 → 建库 → 试答」全流程，验证本地来源的片段可被检索且来源指向原文件路径
- [ ] 13.3 验证混合来源知识库：同一库内网页与本地文件均可被检索命中
- [ ] 13.4 验证测试覆盖率不低于 80%，输出覆盖率报告
- [ ] 13.5 编写 README：安装、启动、三个界面的使用步骤、provider 选型指引、本地模型体积说明，验证未参与开发者能照此完成一次建库
- [ ] 13.6 在文档中写明导出服务不含鉴权、默认回环、对外暴露需自行鉴权，验证该约束被明确记录

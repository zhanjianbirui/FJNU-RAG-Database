## 1. 依赖与骨架

- [ ] 1.1 在 `pyproject.toml` 加入 `trafilatura` 并验证 `uv sync --extra dev` 成功、`import trafilatura` 可用
- [ ] 1.2 建立 `src/kbwb/structure/` 模块与 `tests/structure/` 镜像结构，验证 `uv run python -c "import kbwb.structure"` 成功
- [ ] 1.3 在配置模型中加入探测相关项（请求预算上限、模板相似度阈值、每模板采样数、分区置信度阈值），验证缺失或越界时报出环境变量名的单元测试通过
- [ ] 1.4 扩充 `.env.example` 覆盖新增变量，验证 `.env.example` 与配置模型一致性的既有用例通过
- [ ] 1.5 准备真实页面夹具：从 `ccs.fjnu.edu.cn` 保存列表页、详情页、人物页各一份至 `tests/fixtures/`，验证夹具含大体量导航、多个同名 class 容器与正文外长文本块

## 2. 页面分区识别

- [ ] 2.1 先写八类分区的契约测试（类型取自固定集合、每个分区带选择器与置信度），验证测试因未实现而失败
- [ ] 2.2 接入 trafilatura 实现正文分区识别，验证对真实详情页夹具抽出的正文不含标题、发布时间与导航的测试通过
- [ ] 2.3 实现正文识别失败时置信度为零并标记，验证该页面仍可参与模板归类的测试通过
- [ ] 2.4 实现导航、页脚、侧栏分区识别（复用并扩展 `dom.py`），验证三类各自在真实夹具上被正确归类的测试通过
- [ ] 2.5 实现分页分区识别（数字链接簇与分页 URL 形状），验证对真实列表页夹具识别出分页区的测试通过
- [ ] 2.6 实现条目列表分区识别（同一标签路径重复出现达到阈值），验证列表页命中、详情页不误报的测试通过
- [ ] 2.7 实现元信息分区识别（正文之前、含日期或短标题的元素），验证标题与发布日期落在该分区的测试通过
- [ ] 2.8 实现附件分区识别（指向文档类扩展名的链接簇），验证有附件与无附件两种情形的测试通过
- [ ] 2.9 对真实详情页夹具做一次人工核对，验证八类分区的归类结果与人工判断一致

## 3. 页面模板聚类

- [ ] 3.1 实现结构指纹（标签加 class 首词的路径集合）与 Jaccard 相似度，验证同结构页面相似度高于阈值、异结构页面低于阈值的测试通过
- [ ] 3.2 实现结构指纹与 URL 形状的合取聚类，验证「URL 同形但结构不同」不被合并、「跨栏目同模板」被合并两个测试通过
- [ ] 3.3 实现模板记录样本 URL 与覆盖栏目，验证输出的每个模板均带可核对样本的测试通过
- [ ] 3.4 验证按模板采样不重复：多个栏目归为同一模板时只采样一次的测试通过

## 4. 栏目树探测

- [ ] 4.1 实现导航容器定位与栏目树还原（含嵌套层级），验证对真实首页夹具还原出的栏目名为可读中文且层级正确的测试通过
- [ ] 4.2 实现导航不可识别时退回链接聚类，验证栏目名被标记为未知并提示需人工命名、且不以 URL 片段充当名称的测试通过
- [ ] 4.3 实现 sitemap 可用时优先据其建立栏目候选，验证有 sitemap 与无 sitemap 两条路径各自的测试通过
- [ ] 4.4 对 `ccs.fjnu.edu.cn` 实跑栏目树还原并人工核对，验证栏目名称与层级与站点导航一致

## 5. 页面类型与规模估算

- [ ] 5.1 实现页面类型判定（列表页、单页、未知三类），验证三种情形各自的测试通过
- [ ] 5.2 实现无法判定时标记未知并附原因，验证系统不做猜测的测试通过
- [ ] 5.3 实现从条数文本估算规模（覆盖「共 N 条」「共 N 页」「N/M 页」「总数：N」写法），验证各写法命中与普通文本不误报的测试通过
- [ ] 5.4 实现从分页链接最大页码估算规模，验证对真实列表页夹具得出末页页码的测试通过
- [ ] 5.5 实现仅有「下一页」时的倍增试探定位末页，验证试探请求数为对数量级且计入探测预算的测试通过
- [ ] 5.6 实现无法估算时标记未知，验证系统不给推测值的测试通过
- [ ] 5.7 实现估算方式的记录与约数呈现，验证输出携带所采用的估算方式、且不以精确数字呈现的测试通过

## 6. 探测编排与预算

- [ ] 6.1 实现四步探测编排（首页取栏目树、逐栏目判类型与估规模、按模板采样、对齐落盘），验证对真实站点一次探测完成且各步产物齐备的集成测试通过
- [ ] 6.2 实现请求总数预算与达上限中止，验证超预算时停止请求并列出未探测栏目、不静默截断的测试通过
- [ ] 6.3 实现探测全程遵守既有 robots 判定与限速，验证被禁 URL 不发起请求并报告规则的测试通过
- [ ] 6.4 实现单栏目失败隔离，验证某栏目探查失败被记录、该栏目标为未知类型、整次探测不中断的测试通过
- [ ] 6.5 验证探测不写入任何页面正文到语料存储：一次完整探测后语料目录无正文产物的测试通过
- [ ] 6.6 对 `ccs.fjnu.edu.cn` 实跑完整探测并记录实际请求数，验证请求数落在设计预期的数十量级

## 7. 结构库落盘与版本

- [ ] 7.1 定义结构库的持久化格式与加载校验，验证非法结构文件在加载阶段报出字段路径的测试通过
- [ ] 7.2 实现 `<data_root>/<kb_id>/structure/` 布局与版本目录，验证目录创建与路径穿越拒绝的测试通过
- [ ] 7.3 实现版本指针的原子切换，验证写入新版本后指针切换、失败或中断时指针不变两个测试通过
- [ ] 7.4 实现生效版本的读取，验证服务重启后读到中断前的生效版本的测试通过
- [ ] 7.5 验证上一版本被保留：连续两次探测后两个版本目录均存在的测试通过

## 8. 结构差异比对

- [ ] 8.1 实现新增与消失栏目的比对，验证两类变化各自被列出的测试通过
- [ ] 8.2 实现规模变化比对，验证输出变化前后估算值的测试通过
- [ ] 8.3 实现模板改版检测，验证结构显著变化时标注「基于该模板生成的抽取规则可能已失效」的测试通过
- [ ] 8.4 实现无变化时的明确报告，验证不返回空结果让使用者自行判断的测试通过
- [ ] 8.5 以两份构造的结构快照覆盖四类差异，验证比对结果完整且不误报的测试通过

## 9. 范围勾选

- [ ] 9.1 实现按栏目勾选，验证爬取范围仅含所选栏目的测试通过
- [ ] 9.2 实现按页面模板勾选，验证范围仅含属于该模板的页面的测试通过
- [ ] 9.3 实现两个维度取交集，验证同时限定栏目与模板时范围正确的测试通过
- [ ] 9.4 实现空选择时的提示，验证系统不以全站为默认的测试通过
- [ ] 9.5 实现勾选状态的持久化，验证重启后勾选仍在的测试通过

## 10. 抽取推断改造

- [ ] 10.1 把 `selectors.py` 的推断作用域从整站改为按模板，验证两个不同模板各自输出独立选择器集合的测试通过
- [ ] 10.2 把 `sampling.py` 改造为按模板采样，验证共用模板的多个栏目只采样一次的测试通过
- [ ] 10.3 实现未选中模板不推断、不请求，验证结构库中未勾选模板零请求的测试通过
- [ ] 10.4 保留并回归验证既有的命中率语义：命中按真实抽取路径判定、无候选时标记需人工指定、附件选择器只用实际存在的扩展名——三条既有回归测试仍通过
- [ ] 10.5 把 `draft.py` 改造为按勾选范围生成配置，验证草稿含各模板对应的规则组、标明覆盖栏目、且通过站点配置格式校验的测试通过
- [ ] 10.6 实现低置信度字段的提示注释，验证既有用例在新的按模板结构下仍通过

## 11. 爬取范围衔接

- [ ] 11.1 把爬取入参从单份站点配置改为「站点配置 + 已勾选的结构库」，验证按勾选范围爬取的集成测试通过
- [ ] 11.2 实现未勾选任何范围时在发起请求前中止，验证提示需先选择范围的测试通过
- [ ] 11.3 实现越出勾选范围的链接不抓取，验证属于未勾选栏目的链接被跳过的测试通过
- [ ] 11.4 实现结构版本过期时要求确认，验证不按过期结构继续爬取的测试通过
- [ ] 11.5 验证既有合规约束不受影响：robots、限速、User-Agent 三项既有测试仍通过

## 12. 工作台界面改造

- [ ] 12.1 后端新增探测、结构读取、差异比对、勾选四组接口，验证各端点的集成测试通过
- [ ] 12.2 把探测改为后台长任务并支持进度查询，验证关闭页面后任务继续、重开可恢复进度的测试通过
- [ ] 12.3 界面改为展示栏目树与页面模板双维度，验证树中每个栏目显示页面类型、规模约数与字段把握度
- [ ] 12.4 界面实现范围勾选交互，验证勾选结果与后端一致且空选择时给出提示
- [ ] 12.5 界面展示结构差异，验证四类变化各自可见且模板改版被显著标出
- [ ] 12.6 界面展示每个模板的样本 URL 与分区结果，验证使用者能据此核对聚类是否合并有误

## 13. 集成验收

- [ ] 13.1 端到端跑通「探测 `ccs.fjnu.edu.cn` → 查看结构 → 勾选一个栏目 → 推断抽取规则 → 生成配置草稿」，验证草稿通过配置校验且未勾选栏目零请求
- [ ] 13.2 端到端跑通「二次探测 → 差异比对」，验证四类变化在人为改动的快照上被正确报出
- [ ] 13.3 验证探测请求量：对真实站点记录实际请求数并与预算上限对照，确认在数十量级
- [ ] 13.4 验证测试覆盖率不低于 80%，输出覆盖率报告
- [ ] 13.5 更新 README：说明「先探测、再勾选、后爬取」的流程与探测的请求开销，验证未参与开发者能照此完成一次探测与勾选
